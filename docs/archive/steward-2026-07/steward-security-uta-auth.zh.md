# Steward 安全议题 — 同主机信任边界（重新打开：门有两扇）

> **归档状态（2026-07-11）**：保留同主机认证事故、攻击复现和当时决议的历史全文；本文顶部
> 的“重新打开/下一步”不再是当前任务。当前权限边界见
> [../../trading-agent-architecture.zh.md](../../trading-agent-architecture.zh.md)。

> 版本：v1.1（2026-07-06，**复审推翻了"洞已补好"，重新讨论**）
> 地位：**门 #1（UTA 端口）已修并合并（#51 / PR #52）；但 P3-4a 复审发现门 #2（Alice 自己的 Web 端口）还开着——同一个 agent 换一跳照样把钱动了。** 见 §7。§0/§5 记录的是门 #1 的决议（仍然有效，只是不够）；§7 是新发现，等你批注。
> 用法：门 #1 决议见 §0（已执行）。**新问题看 §7，在 §7 末尾用 `>` 批注收敛。P3-4a 再次按住，不提交、不合并。**

---

## 0. 决议（maintainer 已批，2026-07-06）

| 项 | 决定 | 说明 |
|---|---|---|
| **修法** | **A — 给 UTA 交易接口加内部 token 认证** | 复用 #32 的内部 token 模式：Guardian 生成 → Alice 转发带 token → UTA 拒绝无 token 请求 → token 从工作区 agent 环境剥掉。顺带修好 F1。 |
| **排期** | **甲 — 先锁后门，再落 P3-4a** | #51 安全硬化插在 P3-4a 之前。paper 战役也等 #51 落地后再开跑（战役虽只碰虚拟钱，但先夯地基更干净）。 |
| **P3-4a 代码** | **暂不提交，等 #51 后重构续用** | 已实现的 paper auto-push 逻辑保留在 `.worktrees/issue-p3-4a`，核心可复用；#51 落地后把"档位从哪来"改到认证通道上再重新落地。 |

**下一步**：在 `.worktrees/issue-51/` 实现 #51（codex 实现 → 门禁 → sonnet 活体审查 → PR，不自动合并）。

---

## 1. 一句话版本

**一个跑在工作区里的 AI agent，可以绕过我们所有的人工审批设计，直接命令 UTA 下单成交。**

它不需要"push 工具"（我们 P3-1 已经把 push 工具删了），它只要在自己的终端里执行一条网络请求就行。我们所有"人类专属 push""无 push 工具"的保证，挡不住这条路。

---

## 2. 先复习：我们以为的架构长什么样

回顾一下我们一路建立的安全模型（P1/P2/P3）：

```
                     ┌─────────────────────────────────────┐
   AI agent 在       │  Alice 进程                          │
   workspace 里  ──> │  - 给 agent 的工具面（P3-1 裁剪过）  │
   （通过 MCP 工具） │  - 人类专属 push 路由（要登录会话）  │
                     │  - 审批门、审计                       │
                     └───────────────┬─────────────────────┘
                                     │  HTTP 转发
                                     ▼
                     ┌─────────────────────────────────────┐
                     │  UTA 进程（管 broker、真正下单）    │
                     │  - guards 管道、风险状态机           │
                     │  - 只有它能碰 broker 凭证            │
                     └─────────────────────────────────────┘
```

我们一直假设的因果链是：

- agent 只能通过 **MCP 工具**说话（P3-1 裁剪了工具面，删了 push 工具）；
- 要真正成交，必须有人在 **Alice 的 Web UI** 点 push（人类专属路由，要登录会话）；
- Alice 再把请求转发给 UTA，UTA 才碰 broker。

**结论（我们以为的）**：agent 没有 push 工具 ⟹ agent 不能让交易成交 ⟹ 必须人点头。

---

## 3. 实际上的架构——洞在哪

问题出在 **Alice 和 UTA 之间那条线**，以及**工作区 agent 其实站在哪里**。

### 3.1 UTA 对谁都不设防

UTA 的交易接口（包括"人类专属"的 wallet/push、emergency-stop、flatten）**完全不检查请求者是谁**。代码里的原话注释（`src/webui/routes/trading-proxy.ts:6-7`）：

> "has no auth between the two — UTA is bound to 127.0.0.1 only, so the trust boundary is the host, not the request."
> （两者之间没有认证——UTA 只绑在 127.0.0.1，所以信任边界是"这台机器"，不是"这个请求"。）

翻译成人话：**UTA 的设计假设是"能连到我这个本机端口的，都是自己人，照做不误"。** 它不看你是不是登录过、是不是 Alice、是不是人——只要请求打到 `127.0.0.1:<UTA端口>`，它就执行。

这个设计本身在"Alice 和 UTA 都是我们自己的进程"的世界里是合理的。问题是——

### 3.2 工作区 agent 也站在这台机器上

工作区里的 AI agent 不是在一个隔离的盒子里。它是**同一台机器上的一个普通进程**（PTY 终端），它能：

- 访问 `127.0.0.1`（loopback，本机网络，没有任何东西拦它）；
- 读到 UTA 的默认端口号 **47333**——这个号写在 **CLAUDE.md** 里，而 CLAUDE.md 就在 agent 能读到的仓库里（退一步，就算不写，扫一下本机端口也是几秒钟的事）；
- 执行任意 shell 命令（`shell` 适配器直接就是终端；`codex`/`claude` 也能跑命令）。

我们确实在 P3-1/#32 做了一层防护：把 Alice 的工具 URL、MCP URL、内部 token 都从 agent 的环境变量里**剥掉了**。所以 agent 的环境里看不到这些地址。但这挡不住它**知道默认端口 + 直接连 loopback**。

### 3.3 于是这条路是通的

```
AI agent 在 workspace 终端里，直接执行：

  curl -X POST http://127.0.0.1:47333/uta/<账户id>/wallet/push

  （账户 id 它从自己的只读查询工具就能拿到）

UTA 收到 → "来自本机，照做" → 执行待批交易 → 成交。

全程：没有 push 工具、没有登录会话、没有人点头。
```

活体审查已经**实测复现**了这条路（审查员用裸 curl 对一个 live 类型账户打 wallet/push，唯一失败点是我们给的假 broker 凭证被 broker 拒了——也就是说，授权这一关它**根本没被拦**，只是假凭证下不了真单）。

### 3.4 P3-4a 让问题更具体（F1）

P3-4a（paper auto-push）新加的一个点，是同一类问题的新实例：它从 HTTP 请求体里的 `effectiveAuthzLevel` 字段判断"这个 workspace 够不够 paper 档、能不能自动放行"。这个字段可以伪造——裸 curl 灌一个 `effectiveAuthzLevel: "paper"`，auto-push 就开火了。UTA 只用账户侧的上限二次钳制，但 workspace 这一侧的档位是**照单全收 HTTP 请求里写的值**。

好在 P3-4a 的**实盘闸是结构性的、扛住了**（审查 F3：对 live 账户，无论怎么伪造都不会 auto-push），所以 F1 今天的实际杀伤只在 paper（虚拟钱）。但它和 §3.3 的洞是同一类。

---

## 4. 为什么这件事严重到要停下来讨论

我们整个项目的命门，你定的那句话——**"bot 不可信，笼子可信"**。

这个发现说的是：**笼子的锁，本质是"agent 不知道钥匙在哪"，而不是"agent 拿不到钥匙"。** 一旦 agent 知道了（而钥匙就写在它能读的 CLAUDE.md 里），锁就是虚的。

我们在 P1（风险脊柱）、P2（审计链）、P3（授权阶梯）建的每一道人工审批、每一层授权，都默认"agent 只能走 Alice 给的正门"。这个发现说正门旁边有一扇没锁的后门。**不是这些工作白做了**——guards、风险状态机、审计在正门上全部有效；但只要后门开着，一个"想绕"的 agent 可以不走正门。

诚实地说：这个洞是**早就存在的**（人类专属 push 一直建在这个不设防的 UTA 接口上），不是 P3-4a 引入的。P3-4a 只是第一个"让 agent 自主执行"的功能，把这个一直在的假设逼到了台面上——因为一旦我们开始让机器自动执行，"谁能命令 UTA"就从理论问题变成了必须回答的问题。

---

## 5. 怎么修（选项 + 我的推荐，请在这里批注）

### 5.1 推荐方案：给 UTA 交易接口加内部 token 认证

完全复用我们 **#32 已经做过并验证过的模式**（当时给事件接入口 `/api/events/ingest` 加的就是这个）：

1. Guardian（总管进程）每次启动生成一个随机内部 token；
2. Alice 拿到这个 token，每次转发给 UTA 时带上；
3. UTA 拒绝任何**不带正确 token** 的交易请求；
4. 这个 token **从工作区 agent 的环境里剥掉**（就像 #32 已经把 ingest token 剥掉那样，spawn-env 里加一行）。

效果：

- agent 手里没有 token ⟹ 它 `curl` UTA 的 push/kill-switch 路由会被拒 ⟹ **后门锁上了**；
- P3-4a 的 `effectiveAuthzLevel` 变可信（只有认证过的 Alice 能设）⟹ **F1 顺带修好**；
- P1/P2 的"人类专属 push"从"障眼法"变成"真强制"。

代价：中等。改 UTA 路由加认证网关 + Alice 代理带 token + Guardian 生成 token，镜像 #32 的接线。是一个独立 issue（[#51](https://github.com/a-green-hand-jack/OpenAlice/issues/51) 已开）+ 一个 PR 的量。

> 我同意这个想法

### 5.2 其他选项（更重，列出供对比）

- **B. 网络隔离工作区**：让 agent 的进程连不到 UTA 的 loopback 端口（命名空间/沙箱）。更彻底，但平台相关、实现重、和现在"同主机多进程"的架构冲突大。
- **C. UTA 改用受限权限的 Unix socket**：不开 TCP 端口，改 socket + 文件权限。但 agent 通常和 Alice/UTA 跑在同一个用户下，除非做用户隔离，否则权限挡不住——收益有限。

我的判断：**A 最对**，成本可控，且我们已经有 #32 的成功先例照抄，还顺手把 F1 和 P1/P2 的既有保证一起夯实。

### 5.3 排期问题（这是要你定的第二件事）

- **选项 甲（推荐）**：把 #51 安全硬化插在 P3-4a **之前**——先锁后门，再让 P3-4a 在认证通道上重新落地（它的核心逻辑基本能复用，改动主要在"档位从哪来"）。
- **选项 乙**：先接受"paper-only、虚拟钱、洞只在 paper"的现状，**先推进多市场回测战役**（战役全程只碰 paper/mock，虚拟钱，F3 保证实盘不受影响），把 #51 排在"任何实盘之前"的硬门槛上。战役更快开跑，但带着一个已知的 paper 侧后门。
- **选项 丙**：你的其他想法。

> **待你批注：**
>
> 1. 上面对问题的描述，你看懂了吗？有没有哪里还是不清楚，我再展开。
> 2. 修法选 A
> 3. 排期选 甲
> 4. P3-4a 已实现的代码：等 #51 之后重构续用，还是先按现状（paper-only）提交、#51 再回头收紧？
>    1. 已经实现代码等 #51 之后重构续用

---

## 6. 附：审查其余结论（都是好的）

P3-4a 除了 F1/F2，其余 6 项活体验证全过：F3 实盘闸结构性扛住（最重要）、F4 风险状态只在 NORMAL 触发、F5 guard 拒绝正确留痕、F6 幂等不双执行、F7 账户上限钳制、F8 人工 push 无回归 + agent 仍无 push 工具。也就是说：**一旦地基夯实，P3-4a 本身是可信的。**

（注：下面 §7 的复审证明"地基"不止 §5 一层——门 #1 补好后还有门 #2。P3-4a 的自身逻辑仍然是好的，但不能在门 #2 开着时落地。）

---

## 7. 复审推翻结论：门有两扇，我们只锁了一扇

### 7.1 发生了什么

门 #1（#51）合并后，我把 P3-4a 重挂到已认证的通道上（合并零冲突、全绿 2453 测试），再请一个独立审查员做活体复审。**它把结论推翻了：REJECT。** 它没有停在读代码，而是把整条攻击真的跑通了——**零凭证、无人值守，完成了一笔 paper 成交**。

### 7.2 门 #2 在哪

#51 锁的是 **UTA 自己的端口**（`127.0.0.1:<utaPort>`）。这没问题，实测无 token 直连 UTA 的 push/commit 全部 401。**但 agent 根本不用走 UTA 的端口。**

Alice **自己的 Web 端口**（`127.0.0.1:<webPort>`）上挂着 `/api/trading/*` 的 BFF 代理，它会**自动替调用者补上 #51 的内部 token** 再转发给 UTA。而 Alice 的鉴权中间件（`src/webui/middleware/auth.ts:79-84`）有一条**出厂默认就生效**的旁路：

> 只要没配置可信代理（出厂默认就是没配），**任何来自 loopback 的请求，直接放行，跳过 session/cookie 鉴权。**

翻成人话：**Alice 假设"能从本机连到我的人都是自己人"。** 但工作区 agent 恰恰就是"本机上的一个进程"——这正是 #51 commit message 里自己写的那句"an ordinary same-host process with loopback access"。而且 `spawn-env.ts` 剥掉了 `OPENALICE_UTA_URL`/token，却**没剥 `OPENALICE_WEB_PORT`**——Alice 自己的端口号明明白白留在 agent 环境里。

### 7.3 审查员实测跑通的链条（全程零凭证、纯 loopback curl，无 cookie、无 token）

1. `POST /api/trading/config/uta` —— 凭空建一个 mock 账户。**成功。**
2. `PUT /api/trading/config/uta/:id` 把自己的 `maxAuthzLevel` 提到 `paper`（这条路由是我们标称"human-only 授权变更"的那条）。**成功。**
3. `POST /api/trading/uta/:id/wallet/commit`，body 里伪造 `effectiveAuthzLevel: "paper"`，走 **Alice 的端口**。→ auto-push 开火 → 两笔 mock 单 `filled`，持仓从 `[]` 变成 `AAPL x2`，现金 `100000→99800`，pending 清空。

**一次真实成交，链条里没有任何一处出示过凭证，没有任何人点头。**

### 7.4 这说明的根本问题

门 #1 的教训是"UTA 不能假设'能连到我的都是自己人'"。门 #2 是**一模一样的错，只是在 Alice 这一层**：**一旦同主机上跑着不可信的 agent，"loopback = 可信"这个前提就是假的。** 而这条旁路不止放行交易——它放行 Alice **所有** `/api/*`（配置、凭证保险库里的模型 API key、工作区管理……）。也就是说 agent 能零凭证读到的，可能远不止交易一个面。

诚实说：这条旁路是**早就存在的**（跟 F2 一样，不是 #51 或 P3-4a 引入的）。**是 P3-4a 把它从"能读"升级成"能自己动钱"**——因为 auto-push 让一个伪造的 `commit`（不需要单独的 push 动作）就足以成交。所以在门 #2 补好前，P3-4a 不能落地。

我也要诚实认账：**#51 我已经合并了，并且当时跟你说"洞补好了、活体验证过了"——这个信心是过头了。** #51 的活体审查验的是 UTA 端口，也测了"带 cookie 走 BFF"，但**没测"不带 cookie 走 BFF"**——恰恰是那条 loopback 旁路让 cookie 变可选。P3-4a 的审查员多走了一跳，才把真正的门找出来。#51 本身没白做（UTA 端口确实锁上了，是有效的一层，也是 UTA 将来分离到独立设备的必要前提），但它**不是这个威胁的完整修复**。

### 7.5 怎么修（选项 + 我的倾向，请批注）

核心矛盾：怎么区分"本机上合法的浏览器 UI"和"本机上的 agent"？——**浏览器登录后有 session cookie，agent 没有（也不该有）。** 所以修法都围绕"高风险路由即使在 loopback 也必须出示 session"。

- **A（外科手术，我倾向）**：对 `/api/trading/*`（BFF 代理）和 `/api/trading/config/*`（账户增改、`maxAuthzLevel`）这几条**高风险路由，取消 loopback 旁路，强制要求有效 session**。合法 UI 有 cookie 照常用；agent 没 cookie 就被拒。血溅面最小。代价：本机用户对这些操作要先登录（登录流程已存在）。**遗留**：凭证保险库等其他敏感面仍在旁路后——需要同一轮一并评估，还是单列。
- **B（连根拔）**：直接承认"同主机 ≠ 可信"，把 loopback 旁路对**所有敏感 `/api/*`** 关掉，一律要 session。最贴合信任模型，但改动面大、对纯本机单用户的使用习惯冲击大（什么都要先登录）。
- **C（隔离 agent）**：从网络层把 agent 进程与 Alice 的 Web 端口（和 UTA 端口）隔开（命名空间/沙箱），而不是在鉴权层堵。最彻底，但平台相关、结构性改动大，和"同主机多进程"现状冲突。

配套（无论 A/B/C 都建议做）：把 `OPENALICE_WEB_PORT` 也加进 `spawn-env` 的剥离清单（纵深防御；agent 能扫端口，所以这只是加高门槛不是锁）。

> **待你批注（§7）：**
>
> 1. 门 #2 的描述你看懂了吗？（一句话：#51 锁了 UTA 的门，但 agent 走 Alice 自己的门，那扇还开着，实测零凭证跑通了一笔成交。）
> 2. 修法选 A / B / C？其中 A 的"遗留"（凭证保险库等其他敏感面）：这一轮一并修，还是先只修交易面、其余单开 issue？
> 3. 排期：门 #2 修好前 P3-4a 一直按住（我的默认）。认可吗？
> 4. 关于 #51 我已合并这件事：它作为"UTA 端口锁"是有效的一层，我建议**留着不回退**，门 #2 的修复作为它的补全。认可吗？

### 7.6 决议（maintainer 已批，2026-07-06）

| 项 | 决定 |
|---|---|
| **修法** | **A（外科手术）+ 含凭证保险库**。对敏感路由取消 loopback 旁路，即使本机也强制要 session；不做全局 B。 |
| **本轮覆盖的敏感前缀** | `/api/trading`（执行 BFF + `/config` 账户授权/broker 凭证）、`/api/simulator`（paper god-view）、`/api/config`（AI 凭证保险库，`GET /credentials` 明文回传 apiKey）、`/api/workspaces`（含 `PATCH /:id/authz-level` 工作区授权变更 + `/credentials` 凭证）。 |
| **机制** | `auth.ts` 里给这几个前缀开"loopback 豁免的例外"——命中即跳过 loopback 放行、落到 session 校验。浏览器有 cookie 照常；agent 无 session 被 401。cron/headless 派发是**进程内**、不走 HTTP，不受影响；UTA→Alice 的 `/api/events/ingest` 有自己的 #32 token 且不在此列，不受影响。 |
| **纵深防御** | 把 `OPENALICE_WEB_PORT` 也加进 `spawn-env` 剥离清单。 |
| **排期** | 门 #2 修好前 P3-4a 一直按住。#51 留着不回退（有效的一层，UTA 端口锁 + 未来分离的前提）。 |
| **UX 代价（A 的固有代价，已接受）** | 本机用户对交易/配置/工作区操作需先登录（登录流程已存在，SPA 壳 + `/api/auth/*` 均为公开路由，登录页照常渲染）。 |

**下一步**：GitHub issue → `.worktrees/issue-N/` → codex 实现 → 门禁 → sonnet 活体复审（必须实测：零凭证 agent 打这四个前缀全 401、人类登录后照常、headless/autonomous 不回归）→ PR（不自动合并）。之后再把 P3-4a 挂到门 #2 补好的地基上重跑活体审查。
