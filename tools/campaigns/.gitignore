# Campaign run artifacts are orchestrator-side outputs.
runs/

# Curated benchmark cells under cells/ are checked in for reproducibility.
# Use cells/scratch/ for throwaway fetched/anonymized experiments.
cells/scratch/
